@using LibraryService
@model IEnumerable<ItemEDV>

@{
  ViewBag.Title = "Информация по ЕДВ";
}

<div class="panel panel-headline">
    <div class="panel-heading">
        <h2 class="panel-title">Информация по ЕДВ</h2>
    </div>
    <div class="panel-body">
        <div class="container-fluid">
            @*public string LastName { get; set; }
                public string FirstName { get; set; }
                public string MiddleName { get; set; }
                public string DateBr { get; set; }*@
            <div class="row">
                @using (Html.BeginForm("ListEDV", "ListSoc", FormMethod.Post, new { @class = "formsearch mb20" }))
                {
                <div class="col-md-4 form-group">
                    @Html.TextBox("LastName", "", new { @class = "form-control", placeholder = "Фамилия" })
                </div>
                <div class="col-md-4 form-group">
                    @Html.TextBox("FirstName", "", new { @class = "form-control", placeholder = "Имя" })
                </div>
                <div class="col-md-4 form-group">
                    @Html.TextBox("MiddleName", "", new { @class = "form-control", placeholder = "Отчетство" })
                </div>
                    <div class="col-md-4 form-group">
                        <label for="DateBr">Дата рождения</label>
                        @Html.TextBox("DateBr", "", new { @class = "form-control", type = "date" })
                    </div>
                    <div class="col-md-12 form-action mb20">
                        <button type="submit" class="btn btn-primary">Найти</button>
                        @Html.ActionLink("Отменить", "ListEDV", null, new{@class = "btn btn-default"})
                    </div>
                }
            </div>
        </div>
    </div>
</div>
<div class="panel panel-headline">
    <div class="panel-body">
        <div class="row">
            <div class="col-md-12">

                <table class="table table-striped table-hover table-responsive">
                    <thead>
                        <tr>
                            <th>Фамилия</th>
                            <th>Имя</th>
                            <th>Отчество</th>
                            <th>Дата рождения</th>
                            <th>Категория</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var p in @Model)
                        {
                        <tr>
                            <td>@p.LastName</td>
                            <td>@p.FirstName</td>
                            <td>@p.MiddleName</td>
                            <td>@p.BrDate.ToShortDateString()</td>
                            <td>@p.Category</td>
                        </tr>
                        }


                    </tbody>
                </table>

            </div>
        </div>

    </div>
</div>

